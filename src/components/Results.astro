<style>
  p {
    white-space: pre-line;
    background-color: color-mix(in srgb, var(--background-color), white 10%);
    border-radius: 0.5rem;
    padding: 1.5rem;
    margin: 0;
    line-height: 1.8;
  }

  .timezone-note {
    font-size: 0.85em;
    margin-top: 0.5rem;
    color: color-mix(in srgb, var(--foreground-color), transparent 30%);
  }
</style>

<div id="results-container" style="position: relative;">
  <button
    id="shareResults"
    class="copy-button"
    style="position: absolute; top: 0; right: 0;"
  >
    <span><i class="fas fa-copy"></i></span>
    <span class="text">Copy</span>
  </button>

  <p id="results">Checking the weather...</p>
</div>

<script src="@scripts/results.ts"></script>
