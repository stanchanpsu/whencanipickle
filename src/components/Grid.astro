<style>
  #grid {
    display: grid;
    grid-template-areas: "results calendar" "share calendar" "map calendar";
    grid-template-columns: 1fr 1fr;
    grid-template-rows: auto auto;
    gap: 0rem 1rem;
    align-items: start;
  }

  @media (max-width: 700px) {
    #grid {
      grid-template-areas: "results" "share" "calendar" "map";
      grid-template-columns: 1fr;
    }
  }
</style>

<div id="grid">
  <slot />
</div>
